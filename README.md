# `@cettox/prettier-config@1.0.0`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @cettox/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@cettox/prettier-config"
}
```
